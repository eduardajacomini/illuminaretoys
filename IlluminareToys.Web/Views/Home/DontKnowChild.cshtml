@model IEnumerable<GetProductCategoryOutput>

@{
    Layout = "~/Views/Shared/AdminLTE/_Layout.cshtml";
    ViewData["Title"] = "Catálogo - Não Conheço a Criança";
}

<div class="buttons-container">
        @foreach (var item in Model)
        {

            <a class="button-menu" asp-action="DontKnowChildCategoryProducts" asp-route-category="@item.Description">
                <h3 class="button-menu-label">@item.Description</h3>
            </a>

            @*<div class="col-lg-3">
                <div class="card card-button mx-auto">
                    <a class="card-body" asp-action="DontKnowChildCategoryProducts" asp-route-category="@item.Description">
                        <h3 class="display-5 text-center card-button-label">@item.Description</h3>
                    </a>
                </div>
            </div>*@
        }
</div>


<style>
    .buttons-container {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
    }

    .button-menu {
        padding: 20px;
        background-color: #f012be;
        border-radius: 10px;
        box-shadow: 0 0 2px #888;
        text-align: center;
        margin-bottom: 10px;
        width: 80%;
    }

        .button-menu:hover {
            cursor: pointer;
            opacity: 0.7;
        }

    .button-menu-label {
        color: white;
    }
</style>