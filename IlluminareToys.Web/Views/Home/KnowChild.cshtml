@model IEnumerable<GetGroupOutput>

@{
    Layout = "~/Views/Shared/AdminLTE/_Layout.cshtml";
    ViewData["Title"] = "Illuminare Toys - Catálogo - Conheço a Criança";
}

<div class="buttons-container">

        @foreach (var group in Model)
        {
            @*<div class="col-lg-3">
                <div class="card card-button mx-auto">
                    <a class="card-body" asp-action="KnowChildAges" asp-route-groupId="@group.Id">
                        <h3 class="display-5 text-center card-button-label">@group.Name</h3>
                        <span class="text-muted text-center text-white">@group.Description</span>
                    </a>
                </div>
            </div>*@

            <a class="button-menu" asp-action="KnowChildAges" asp-route-groupId="@group.Id">
                <h3 class="button-menu-label">@group.Name</h3>
                <span class="text-muted text-center text-white">@group.Description</span>
            </a>
        }
</div>


<style>
    .buttons-container {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
    }

    .button-menu {
        padding: 20px;
        background-color: #f012be;
        border-radius: 10px;
        box-shadow: 0 0 2px #888;
        text-align: center;
        margin-bottom: 10px;
        width: 80%;
    }

        .button-menu:hover {
            cursor: pointer;
            opacity: 0.7;
        }

    .button-menu-label {
        color: white;
    }
</style>